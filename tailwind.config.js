/** @type {import('tailwindcss').Config} */
module.exports = {
  // mode: process.env.NODE_ENV ? 'jit' : undefined,
  mode: 'jiit',
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
